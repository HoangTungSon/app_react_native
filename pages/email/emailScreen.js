import React, { useState } from 'react';
import { View, FlatList, Text, ActivityIndicator } from 'react-native';

export default function EmailScreen({ navigate }) {
    return (
        <View>
            <Text>This is a email screen</Text>
        </View>
    )
}